# 잔여 사용량 안에서의 전수 자동점검과 위험 문항 개선

사용자는 2026-09-09 08:35 KST 이후 기존 전수 의미검토 계획을 재검토하고, **전체 자동점검 + 오답 위험이 높은 문항 집중 개선**을 선택했다. 이전 전수 Sol 실행 계획을 이 문서의 범위와 예산으로 대체한다. 실행 담당은 Root이며, 제한된 독립 문제 검토에만 네이티브 Sol 에이전트를 사용한다.

## 목표와 완료 기준

기존 1,730건 풀이를 보존하고, 모든 문항에 자동점검 기록을 만든다. 자동점검과 기존 심층 판정에서 발견한 위험 중 실제 답 또는 Evidence를 바꿀 가능성이 높은 문제를 우선 개선한다. 완료 산출물은 1,730행 팀 전달 CSV/JSONL, 문항별 자동점검 결과, 실제 의미검토 범위, 승인된 변경과 남은 위험, 사용량 기록이다. 자동점검만 수행한 문항을 전수 의미검토 완료로 표시하지 않는다.

119건의 완료된 심층 판정과 이미 수락한 추가 검토는 재사용한다. 현재 119건 내부 분류는 전 축 지지 9건·조건부 91건·미확정 19건이다. 이는 공식 정답률이 아니다. 조건부 문구만 없애기 위한 재검토와 실제 점수에 영향을 주지 않는 반복 정리는 하지 않는다.

## 비용 판단과 이번 반성

100건 Ultra 독립 수락 시점의 계정 사용량 감소는 22%p였다. 같은 경로의 단순 전수 환산은 `1730 / 100 * 22 = 380.6%p`로, 잔여 예산에 맞지 않는다. 이 구간에는 준비·실패·정정·감사와 계정 공유 사용량이 포함되므로 Sol의 단가로 그대로 사용할 수는 없다. 반대로 Sol이 더 저렴할 것이라는 기대만으로 전수 실행을 승인해서도 안 된다.

실제 Sol의 두 5건 묶음은 모두 strict 완료했지만, 첫 묶음에는 문항 누락·Evidence 과잉·비선택 문단 인용 오류가 있었다. 후속 지침은 알려진 문항 누락 회귀를 통과했으나, 새로운 5건에도 Evidence의 구분 콜론 포함 의심 사례가 있어 의미·형식 수락을 별도로 확인해야 한다. 5건 전후 계정 정수 표시가 같다고 비용 0으로 계산하지 않는다.

실수는 고비용 검토량과 예산을 먼저 연결하지 않은 것, 생성 완료와 실제 내용 수락을 혼동할 여지를 남긴 것, 검토 체계·캐시 구현에 시간을 쓰며 실제 답 개선 효율을 늦게 확인한 것이다. 앞으로는 예산과 개선 가능성을 먼저 고정하고, 이미 확인한 사실을 반복 작성하지 않으며, 변경되는 필드에 필요한 검증만 수행한다. 모든 문제를 동일 비용으로 다루지 않는다.

## 예산과 실행 제한

현재 계정 잔여량 27%를 재계획 기준으로 기록한다. Root의 보수적인 운영 상한은 **15%p 소비, 잔여 12% 보존**이다. 이는 비용 추정치가 아니라 추가 작업량을 제한하는 예산이며, 이 예산 안에 목표를 반드시 달성한다는 보장은 아니다.

| 구간 | 사용량 배분 상한 | 작업 |
|---|---:|---|
| 계획·전수 자동점검·위험 분류 | 2%p | 기존 검사 재사용, 1,730건 로그와 우선순위 생성 |
| 위험 문항 원문 재판정 | 8%p | 첫 10건 이내로 개선 효과 계측, 전체 최대 30건 |
| 변경 검증·통합·전달 | 5%p | 변경된 답/Evidence의 독립 확인, 전체 파일 정합성 검사 |
| 보존 | 12%p | 이후 사용자 판단과 마감 대응용 |

전체 소비에는 Root·검토자·실패·재시도·계획/구현이 모두 포함된다. 자동점검은 문항별 모델 호출을 하지 않지만, 계획·코딩·관리 대화의 사용량은 포함한다. 계정의 다른 작업과 정수 표시·갱신 지연 때문에 정확한 작업 전용 소비량은 알 수 없다. 동시성은 최대 2로 제한하고, 작은 묶음이 끝날 때 관측값을 갱신한다. 잔여량 12% 이하 또는 해당 구간 상한 도달 시 새 모델 요청을 시작하지 않고 결과와 남은 범위를 정리한다. 리셋 구간을 단순 차감하지 않는다.

## 단계 1 — 기존 결과 고정과 전수 자동점검

1. 기존 승인 CSV/JSONL 및 119건·추가 수락 판정의 경로, 해시, 고유 ID를 고정한다. 최신 수락 판정을 재사용하며 이전 승인본을 덮어쓰지 않는다.
2. 전체 1,730건의 ID·질문·원본 record/PDF 연결, 중복·누락, 답 표현, Evidence 빈 값·중복·구분자 의심을 점검한다.
3. 기존 안전한 산술 평가기로 지원되는 식을 재계산하고 기록된 결과·최종 답과 비교한다. 시각·기호식·단위 변환·지원하지 않는 표현은 미검산 사유로 남기며, 파서 실패를 오답으로 단정하지 않는다.
4. Evidence와 선택 인용의 원문 OCR 대조를 위험 탐지에 이용한다. OCR 불일치는 판독 요청을 만들 뿐 원본 PDF와 불일치한다는 확정 근거가 아니다. OCR에서 찾았다는 사실도 의미적 충분성의 증명이 아니다.
5. 새 풀이를 생성하지 않고 자동점검 결과와 위험 목록을 만든다. 이미지 재렌더·캐시 기능 개발·전체 문항 모델 호출은 이 단계에 필요하지 않다.

## 단계 2 — 위험 순위와 제한된 의미검토

우선순위는 예상 오답 위험과 새 근거로 해결할 가능성이다. 범주가 겹치면 한 ID는 한 번만 배정한다.

1. 미확정/null 진단, 실제 시나리오 누락, 질문과 선택 문단 불일치 가능성. 기존 19건을 다시 모두 풀기보다 보류 이유와 확보된 원문을 먼저 분류한다.
2. 정확한 산술 불일치, 수치·부호·단위 판독 충돌, 답 후보를 바꿀 가능성이 있는 조건 누락.
3. Evidence 누락·과잉·ID 전사 문제. 이미 계산이 맞으면 전체 풀이를 재생성하지 않고 해당 근거와 연결을 확인한다.
4. 답에 영향을 주지 않는 물리 가능성·설명 문구는 기존 사유를 유지하고 뒤로 미룬다.

첫 최대 10건은 구간 예산 2%p 안에서 처리하며, 새 근거로 수정 가능한 사례 수와 소요 사용량을 기록한다. 이후 동일 비율을 보장한다고 추정하지 않고 남은 예산에 맞춰 전체 최대 30건까지만 확대한다. 실제 개선 근거가 없으면 같은 문제를 반복 호출하지 않는다. 한 조건의 실패를 재시도하려면 입력 전달 문제 수정이나 새 원문 확인 등 변경 이유가 있어야 한다.

모델은 Sol/high를 우선 사용한다. 기존 전체 OCR에서 실제 인물·행동·요구량으로 후보를 찾고, 필요한 원본 페이지를 확인한 뒤 정확한 계산과 최소 Evidence를 기록한다. 목표 질문과 원문 전체를 이용할 수 있게 유지하되 처음부터 모든 페이지 이미지를 모든 검토자에게 반복 전달하지 않는다. 기존 답을 숨기는 첫 판독과, 저장 후 과거 판정 비교를 구분한다. 입력 축소의 절감률은 미측정으로 남긴다.

## 단계 3 — 변경 검증과 전달

답 또는 Evidence 변경, 새 보류 해소는 변경된 현재 판정과 필요한 원문에 대해 독립적으로 확인한다. 선택 문단이 달라지면 질문·계산·Evidence·상태를 함께 검토한다. 값과 판정이 같은 결과는 기존 승인 기록을 보존하고 독립 검토 이력만 연결한다. 다중 모델 투표나 동일 결과 반복은 독립 원문 확인을 대신하지 않는다.

팀 CSV에는 질문·검증 가능한 풀이·답·Evidence 외에 자동점검 상태, 실제 의미검토 여부, 변경 근거와 남은 보류를 명시한다. 진단용 null을 제출 기권으로 자동 전환하지 않는다. 문항별 모델 초안, 내부 수락, 공식 정답을 구분한다. 최종 1,730개 고유 ID와 CSV/JSONL 대응·수치 표현·원본 연결을 검증한다.

## 재계획 시작 당시의 정지·재사용 상태

전수 Sol 대량 실행과 캐시 개발은 중단했다. 마지막 신규 5건은 중단 요청 전에 이미 반환됐으며 strict·구조 검사를 통과했지만 의미검토 승인은 아직 없다. 진행 중이던 검토자와 캐시 수정자는 중단했고, 미완성 파일은 그대로 보존한다. 기존 이미지·PDF·119건 완료본·100건 체크포인트는 변경하지 않았다. 캐시 도구의 미완료 수정은 이 계획의 선행 조건이 아니며 사용하지 않는다.

로컬 실행 기록과 자동점검 결과는 `artifacts/heldout-rereview-v3-budget/`에 저장한다. 공개 문서에는 집계와 절차만 기록하고 문항별 결과·계정 원시 응답은 로컬 artifact로 유지한다.

## 예산 재계획 수행 결과 — 2026-09-09 KST

전체 1,730건 자동점검과 20건 집중 원문·비교 검토를 완료했다. 별도 형식 점검 2건은 의미검토 수에 포함하지 않는다. 새 판정 14건을 독립 수락했고, 기존 결과 유지·미수락 검토 이력 5건 및 잘못된 답 후보 기각 1건을 기록했다. 이번 시작점 대비 팀 전달 후보 10행이 바뀌었으며 답 문자열 8건·Evidence 집합 9건은 서로 겹친다. 기존 null 재검토 8건은 조건부를 포함한 수치 후보로 바뀌었다. 변경 수는 정답률 개선 수가 아니다.

최신 계약으로 독립 수락된 전체 고유 기록은 126건(지지 15·조건부 99·미확정 12)이다. 나머지 1,604건은 기존 결과를 재사용했으며 전수 의미검토를 주장하지 않는다. 전달 CSV의 메인 풀이도 별도 자동 검사했다: 지원되는 식 3,595개 일치, 320개 미검산, 정확 불일치 0개. 자동점검 11개·수락 계약 19개 회귀 검사와 CSV/JSONL 대응·해시·ID 검증을 통과했다.

메인 답의 진단 문구 7건 중 3건은 Evidence도 비어 있다. 합집합은 7건이며 제출 준비 완료 파일이 아니다. 최신 판정의 null을 제출 기권으로 자동 전환하지 않았고 포털에 제출하지 않았다. 전체 기록과 실제 검토 범위, 미수락 후보와 보류 근거는 로컬 `artifacts/heldout-rereview-v3-budget/exports/`에 보존했다.

사용량은 2026-09-09 10:04:09 KST 기준 27% → 24%, 계정 전체 3%p 감소다. 약 90분 구간이며 정수 표시·계정 공유·준비·정정·통합 비용을 포함한다. 정확한 작업 전용 토큰 수나 금액으로 해석하지 않는다. 15%p 상한·잔여 12% 보존 범위 안에서 제한된 계획을 종료했고, 전수 Sol 호출과 캐시 개발은 재개하지 않았다.
