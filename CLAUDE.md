# CLAUDE.md

이 문서는 Claude 기반 에이전트가 이 저장소에서 작업할 때 따라야 하는 필수 규칙입니다. 과제 배경은 [README.md](README.md)를 함께 확인합니다.

## 필수 작업 흐름

1. 변경 전에 GitHub Issue가 존재하는지 확인하고 이슈 번호를 작업 추적 번호로 사용합니다.
2. 부모 이슈 브랜치는 `feature/<이슈번호>` 형식을 사용합니다. 예: Issue #1 → `feature/1`.
3. 하위 작업 브랜치는 `feature/<이슈번호>-<하위순번>` 형식을 사용합니다. 예: `feature/1-1`.
4. 하위 브랜치는 반드시 부모 브랜치에서 분기합니다. `feature/1-1`의 base와 PR 대상은 `feature/1`이어야 합니다.
5. 부모 브랜치의 PR 대상은 `main`입니다.
6. Issue → Branch → Commit → PR 흐름을 생략하지 않습니다. 문서 변경도 동일합니다.

## 커밋 규칙

- 같은 의미 단락의 변경은 파일 수와 무관하게 하나의 커밋으로 묶습니다.
- 서로 다른 주제는 반드시 별도 커밋으로 분리합니다.
- 기존 커밋과 같은 의미의 후속 보정은 새 독립 커밋으로 남기지 않습니다. `fixup` 커밋을 만든 뒤 interactive rebase 또는 autosquash로 원래 커밋에 합칩니다.
- 원격 작업 브랜치의 커밋을 rebase한 뒤에는 `--force`가 아니라 `--force-with-lease`로 갱신합니다.
- 커밋 제목과 본문은 한국어로 작성합니다.
- 커밋 제목은 반드시 `[#이슈번호]`로 시작합니다. 예: `[#1] 저장소 작업 규칙 추가`.

## Markdown 작성 규칙

- 일반 문단은 문단 안에서 임의로 줄바꿈하지 않고 한 줄로 작성합니다.
- 제목, 목록, 표, 코드 블록 또는 의미상 필요한 문단 경계에서만 줄을 나눕니다.
- lint의 줄 길이를 맞추기 위한 강제 줄바꿈을 추가하지 않습니다.

## PR 규칙

- 모든 작업은 PR을 생성합니다.
- 하위 작업 PR은 부모 브랜치를, 부모 이슈 PR은 `main`을 대상으로 합니다.
- 저장소 소유자의 명시적인 허락 없이는 PR을 병합하지 않습니다.
- PR 생성 요청이나 작업 완료 요청을 병합 허락으로 해석하지 않습니다.
- `main`에 직접 커밋하거나 작업 브랜치를 직접 병합하지 않습니다.

## 초기화 예외

빈 저장소에서 PR base를 만들기 위한 파일 없는 최초의 빈 커밋만 `main` 직접 생성을 허용합니다. 기준 브랜치가 생성된 뒤에는 이 예외를 다시 적용하지 않습니다.
